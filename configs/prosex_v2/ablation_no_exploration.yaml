# Ablation: No Exploration (Deterministic Scheduler)

version: "2.0.0"
experiment_name: "ablation_no_exploration"

# Exploration disabled
eabs:
  exploration_ratio: 0.0

# Inherit other defaults from default.yaml
